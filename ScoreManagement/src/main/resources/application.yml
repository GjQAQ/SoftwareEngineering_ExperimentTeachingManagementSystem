server:
  port: 8083

spring:
  datasource:
    username: root
    password: HCC.mysql.pw#0
    url: jdbc:mysql://120.78.65.145:3306/score_sys
    driver-class-name: com.mysql.cj.jdbc.Driver
#
#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.type.discriptor.sql.BasicBinder: trace

management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
